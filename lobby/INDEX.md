# lobby — kol-mirror

Intake queue for **kol-mirror**: the mirror surface — UI issues, app-shell and
routing behaviour, and this repo's consumption of the `@kolkrabbi/*` packages.
Not documentation — a work queue, deliberately outside `docs/`.

**This file is the ledger. The ledger is the truth, never a raw `ls`.**

| | |
|---|---|
| file one | `clip-drop.sh --kol-mirror NAME` |
| read it | `/lobby-list` · `bin/lobby` · `prefix Ctrl+K` |
| the spec | `~/.dotfiles/docs/operations/systems/lobby/` |

## States

| | state | means | lives in |
|---|---|---|---|
| 🔵 | `filed` | captured, unread | `inbox/` |
| 🟡 | `read` | understood — the row below restates it | `inbox/` |
| 🟠 | `addressed` | a change shipped that is *meant* to close it | `inbox/` |
| 🟢 | `closed` | met the bar; resolution appended | `done/` |
| ⚪ | `parked` | deliberately not-**now**, reason recorded — revisitable | `archive/` |
| ⚫ | `retired` | closed without a fix, not-**ever** — terminal, and never ages | `archive/` |
| 🔴 | `needs-ruling` | **flag, not a state** — blocked on the user's call | wherever it is |
| 📌 | `remainder` | **flag, not a state** — closed at its destination, still owed **here** | `outbox/` |

**`read` is never `closed`.** Understanding a ticket ships nothing.
**Bar for 🟢 closed in this repo — purpose served:** the change shipped and was
verified by running it, cited by file. **The agent closes on that evidence.**
Parking, declaring stale, reopening and any design decision stay the **user's call**.

## Queue — 0 entries
The queue holds LIVE TASKS ONLY. Both settings tickets closed 2026-09-01 on the
first browser pass this repo has ever had.

| | Entry | About | Staged | State |
|---|---|---|---|---|

## Closed

| | Entry | About | Staged | Closed | State |
|---|---|---|---|---|---|
| 🟢 | [SettingsMastheadCluster](done/SettingsMastheadCluster.md) | `SettingsScaffold` owns the masthead row — `picker` · `themeToggle` · gear. This repo passed **nothing** into `header.actions` | 2026-08-30 | 2026-09-01 | `closed` — **seen on screen** at 390×844 and on a production build at shell 0.36.0 · theme 0.122.0 · component 0.153.0. The toggle renders right-aligned on the subtitle baseline, fxr's approved row; `picker` + gear omitted by argument, not defect. kol-mirror was the first adopter anywhere, so the ticket's own "the first to adopt is the check" is served |
| 🟢 | [ShellSettingsPairRetired](done/ShellSettingsPairRetired.md) | kol-shell 0.21.0 dropped `SettingsSection` + `LabelRow`; kol-component's `LabeledControlSection` + `SettingsRow` are the survivors | 2026-08-30 | 2026-09-01 | `closed` — the swap shipped 2026-08-30 under the forcing bump; both deliberate render changes now **confirmed on screen**: section headers are eyebrows (`kol-eyebrow text-fg-80`), row labels uppercase with `align="fill"` holding the four prose rows left |
| 🟢 | [ShellHomeSystemAdoption](done/ShellHomeSystemAdoption.md) | The shared app tier adopted on kol-fxr's wiring: Home + Library on `CatalogPage`, Settings on `SettingsShortcuts` / `SettingsLinks` / `SettingsColophon`, `AppShell railToggleKey` + `nav-*` + logomark from the DS, the theme boot script, `voice="mono"`, no `GridCard` / `TabStrip` left; `touch` stays default (ARCHITECTURE §2) | 2026-08-27 | 2026-08-27 | `closed` — kol-shell **0.10.0** · component **0.108.0** · theme **0.72.0** · icons **0.22.0**; seen rendering on the user's server (session 44, shell 0.8.0), today's bump build green, lint unchanged |

## Archived

_(none yet — ownership, deferral and context notes land in `archive/`)_

## Filed elsewhere

Tickets this ledger does **not** govern — each row names the destination ledger
that does. The **Remainder** is this repo's to do; the state is theirs to report.

| | Receipt | Destination | Last known | Remainder here |
|---|---|---|---|---|
| 🟢 | [ContentFiltersViewStripOverflow](outbox/ContentFiltersViewStripOverflow.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-02 — **kol-component 0.163.0**: `viewPlacement` in `layoutPlacement`'s idiom, the view strip takes its own wrapping line under the divider below md | **none** — bumped, the `.library-catalog` rule and class deleted, Library re-checked at 390 and 1280 |
| 🟢 | [ShellRailCollapsedWithTapOpen](outbox/ShellRailCollapsedWithTapOpen.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-shell 0.38.0 + kol-theme 0.123.0**: under `(pointer: coarse)` the rail's edge draws a 24px tap disc beside the fine-pointer drag, so `shell` is finally collapsed AND openable | **none** — `touch={isHome ? 'shell' : 'drawer'}` shipped the same day; home renders the collapsed 48px rail, every other route keeps the folded drawer. The tap is confirmed present, not confirmed felt — that needs the handset |
| 🟢 | [ShellDrawerOpenOnUnstableDep](outbox/ShellDrawerOpenOnUnstableDep.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-shell 0.37.1**: the 0.37.0 regression where `drawerOpenOn` defaulted to a fresh `[]` inside its own effect's dep array and closed the drawer on every render | **none** — the frozen `DRAWER_OPEN_ON` workaround deleted from `src/App.jsx`; verified the trigger opens and STAYS open at 390 |
| 🟢 | [ContentCardExpandedSplitStacks](outbox/ContentCardExpandedSplitStacks.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-component 0.156.0**: the expanded cell stacks below md, media on top at its ratio, content full width | **none** — bumped and re-measured the same day: prose column **126 → 348**. The 492px media block that remains is OUR missing `/previews/modules/`, not the DS's |
| 🟢 | [ShellDrawerOpenOnRoute](outbox/ShellDrawerOpenOnRoute.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-shell 0.37.0**: `drawerOpenOn` paths whose ENTRY opens the drawer; the close-on-navigate stays for every other path | **none** — `drawerOpenOn={['/']}` passed the same day; home lands with the rail out, `/studio` lands `data-rail-drawer="closed"` |
| 🟢 | [SliderCoarsePointerHeight](outbox/SliderCoarsePointerHeight.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-theme 0.122.0**: `touch-action: pan-y` on the input and 24 → 44 under `(pointer: coarse)`, plus the `.control-slider` row lift we would have missed (a 44px input inside a fixed 24px row overflows it wherever `rowHeight` is not passed inline) | **none** — both carries deleted, `mirror-overrides.css` back to the accent bind and the nav rule; the coarse branch is verified loaded and gated, not verified under a thumb |
| 🟢 | [ShellDrawerBottomSide](outbox/ShellDrawerBottomSide.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-component 0.153.0**: full-width, slides from +100% Y, `height` where the sides take `width`, foot padded past `env(safe-area-inset-bottom)`; ONE detent, stated rather than half-built | **none** — swapped the same day, measured at **y 270 / h 574 = exactly 68dvh** at 390×844; the collapsed 56px bar stays local as the second detent and the opener |
| 🟢 | [SettingsShortcutsGridColumns](outbox/SettingsShortcutsGridColumns.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **kol-shell 0.36.0**: the 0.33.0 ceiling idiom with a 150px floor, row-flow below md, `gap-x-6 md:gap-x-12`; they swept the shell for `grid-cols-` after it and this was the last | **none** — nothing was carried; re-measured at 390, two readable columns (`STUDIO` · `DESK CANVAS`), labels legible, slivers gone |
| 🟢 | [SliderTrackVariableNotForwarded](outbox/SliderTrackVariableNotForwarded.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — **kol-theme 0.98.0 + kol-component 0.134.0**: `.slider-black` reads `var(--kol-slider-track, var(--kol-fg-64))` so the variable inherits, and `style` is forwarded on both branches | **none** — cascade rule deleted, the pin moved onto the adapter's `style` where a call site's own `style` can still win |
| 🟢 | [SliderDualThumbAndPlayhead](outbox/SliderDualThumbAndPlayhead.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — **kol-component 0.133.0 + kol-theme 0.97.0**: all three seams, `variant="dual"` with the clamp in the component, the whole rail seeking, `readout` and alt-click reset on Slider **and** RotaryDial | **none** — adopted the same day, 18 call sites moved with **no edit**, `atoms/Slider.jsx` is now an adapter pinning `readout="value"`, 183 lines of JSX + 157 of CSS retired to `_tmp/2026-08-30-slider-upstream/` |
| 🟢 | [DropdownOptionHoverPreview](outbox/DropdownOptionHoverPreview.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — component **0.125.0**, the contract as filed; keyboard hover declined | **none** — adapter un-stripped, FxUnit needed no change; one on-screen confirmation outstanding |
| 🟢 | [DropdownGhostWidthAndListHeight](outbox/DropdownGhostWidthAndListHeight.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — component **0.124.0** · theme **0.87.1**: `.kol-dd-root` caps at 100% **and** floors `min-width: 0` (the chevron fix; 0.87.0 alone was a half-fix on a flex child), `maxRows`, `rowHeight`, no hover | **none** — the `onOptionHover` half was filed separately as [DropdownOptionHoverPreview](outbox/DropdownOptionHoverPreview.md) and closed the same day; both CSS carries deleted, `rowHeight` forwarded at 24 |
| 🟢 | [RailFlatGrabOpen](outbox/RailFlatGrabOpen.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — kol-shell **0.16.0** · kol-theme **0.88.0**; the flat rail + r2b2 grab are the DS's | **none** — AppShell mounted, local rail + CSS retired to `_tmp/2026-08-28-rail-upstream/`, `kol-framework.css` import dropped, drag verified on screen at 264 |
| 🟢 | [MediaClientBucketTable](outbox/MediaClientBucketTable.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — **kol-media-client 0.3.0**: `KOL_BUCKETS` exported, `buckets` takes `null` \| `true` \| an object merged onto it, `proxied()` reads each bucket's own `proxy` flag | **none** — on 0.3.2, `createMediaClient({ buckets: true })` at `useMediaLibrary.js:26`, no local table left in `src/` |
| 🟢 | [KolSourcesPnpmResolution](outbox/KolSourcesPnpmResolution.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-15 — **theme 0.42.2**, twin `@source` lines per package | none — local override deleted from `src/index.css`, overlay re-verified; built CSS +8.9 kB as the other eleven packages' utilities finally emit here |

## History

| Date | Event |
|---|---|
| 2026-08-15 | **AppShellSet's remainder is served — both halves.** kol-shell 0.1.0 adopted (8 local shell files → `_tmp/2026-08-15-shell-upstream/`, plus PageShell + SettingsScaffold + TabStrip taken up), and the dead `Workshop*` shell swept — `WorkshopLayout.jsx` + `WorkshopSidebar.jsx`, zero references, retired to `_tmp/2026-08-15-shell-upstream/workshop-shell/`; lint fell 122→120 errors with them. The sweep was **missed on the first pass and caught by the user**: the adoption ran off a chat-level plan without this ledger being read, so only the half that had been discussed got done. `KolSourcesPnpmResolution` filed to kol-ds-ui the same day — receipt above |
| 2026-08-15 | lobby created and registered in `~/.dotfiles/files/folders.md` § `lobby`. Flag `--kol-mirror` falls out of the path; no `/lobby-<name>` skill until this repo starts receiving tickets from elsewhere often |
| 2026-08-15 | **Work owed here is recorded in another repo's outbox.** `kol-monitor/lobby/outbox/AppShellSet.md` — 🟢 closed in kol-ds-ui 2026-08-14 (kol-shell 0.1.0 + theme 0.41.0 + framework 0.20.0) — carries a 📌 remainder and names kol-mirror as sharing it: adopt the DS shell, retire the local copies to `_tmp/`, plus this repo's dead `Workshop*` shell sweep. **No receipt stub was written into this repo's `outbox/`** — that filing was kol-monitor's, and inventing a second stub would fabricate a filing this repo never made. Whether kol-mirror gets its own copy is the user's call |
| 2026-08-27 | **`ShellHomeSystemAdoption` filed** from kol-fxr — the shared Home · Library · Settings tier is in the DS; adopt it the way fxr did the same day. |
| 2026-08-27 | **`ShellHomeSystemAdoption` closed** → `done/`, receipt returned to kol-fxr's outbox. Adopted in session 44 on fxr's four files; closed after today's bump to kol-shell 0.10.0 · component 0.108.0 · theme 0.72.0 · icons 0.22.0 (`pnpm outdated` empty). The 0.9.0 `combo` fix retired the local `SHORTCUT_SHEET` map; the `SettingsShortcuts` combo wrap is fixed upstream in 0.10.0 — no DS ticket |
| 2026-08-28 | **`RailFlatGrabOpen` filed to kol-ds-ui** — the rail rebuilt here as the flat 48px column with the r2b2 grab (user ruling, session 46: "obviously go through the DS"). Local `NavRail.jsx` + `.rail-grab` carried until the release; receipt above carries the 📌 |
| 2026-08-28 | **`DropdownGhostWidthAndListHeight` filed to kol-ds-ui** — the local 324-line dropdown retired for the DS one at 29 call sites; four defects surfaced by the studio's Source picker, two of them unfixable from a consumer. Adapter + two CSS carries stay here; the blend-mode hover preview is lost until the DS ships a seam |
| 2026-08-28 | **`DropdownGhostWidthAndListHeight` closed same-day** → component 0.124.0 · theme 0.87.0. Both CSS carries deleted, `rowHeight` forwarded at 24. One remainder stands: `onOptionHover` was filed as "not an ask" and the blend-mode hover preview is still lost |
| 2026-08-28 | **`DropdownOptionHoverPreview` filed to kol-ds-ui** — the blend-mode hover preview, the one feature lost in the dropdown swap. Also took theme **0.87.1**: 0.87.0's `max-width: 100%` was a half-fix on a flex child, whose default `min-width: auto` floors it at content width; 0.87.1 adds `min-width: 0` |
| 2026-08-28 | **`RailFlatGrabOpen` adopted** — kol-shell 0.16.0 · kol-theme 0.88.0. `AppShell` is back in `App.jsx`; `navKeys` deliberately not passed because mirror's ⌥1 is HOME, not a rail item. Local `NavRail.jsx` + the rail CSS retired to `_tmp/2026-08-28-rail-upstream/`, and the `kol-framework.css` import dropped with them — the shell no longer needs `.kol-brand-layout` |
| 2026-08-30 | **The stale 📌 on `DropdownGhostWidthAndListHeight` cleared.** Its remainder was `onOptionHover`, which became its own ticket the same day and closed at component 0.125.0 — the receipt stub has read **Remainder here: none** since. Re-verified in the tree, not asserted: no `.kol-dd-panel` rule survives in `src/styles/components.css`, `onOptionHover` is not in the adapter's `DROPPED` list (`Dropdown.jsx:32`), `FxUnit.jsx:158` still stashes/restores/clears. **Outbox 📌-free at this point** — `SliderDualThumbAndPlayhead`, filed later the same day, opens a new one. The only other thing outstanding is the on-screen confirmation on `DropdownOptionHoverPreview`, which waits on the user's dev server |
| 2026-08-30 | **DS tier bumped — component 0.128.0 → 0.131.0 · shell 0.19.0 → 0.19.1 · theme 0.90.0 → 0.96.0.** `pnpm outdated` under-reported all three (component 0.129.0, theme 0.91–0.95 sat under `minimumReleaseAge`); `npm view` was the truth, as [[operations/02-kol-packages]] says. Three versions added to `minimumReleaseAgeExclude`. What landed: **0.130.0** `QuadrantSync` (dev chrome, unused here), **0.131.0 + theme 0.96.0** the content-variant rename — `default`→`file`, `print`→`catalog`, `work`→`showcase`, `typeface`→`showcase layout="canvas"`, **all four old names alias** — and **shell 0.19.1**, `PageHeader actions` no longer growing the masthead 10px. **No call-site change needed**: mirror's four `variant="catalog"` sites (`LibraryPage.jsx:340,341,361`, `MixerPage.jsx:109`) name the alias TARGET, not a retired name. Build green, lint 117 errors / 43 warnings — all in `src/`, the repo's standing baseline, which a dependency bump cannot move (eslint never reads `node_modules`). **Not looked at on screen** — no dev server was running |
| 2026-08-30 | **`MediaClientBucketTable` was in `outbox/` but on no ledger row** — filed and closed 2026-08-28 with the receipt stub written, and the *Filed elsewhere* table never took it. Added above; a raw `ls` of `outbox/` was the only place it existed, which is exactly the read this ledger exists to replace. Remainder verified done in the tree |
| 2026-08-30 | **`SliderDualThumbAndPlayhead` filed to kol-ds-ui** — the user asked whether a local CSS file is compliant for a `/core` KOL consumer. On sliders it is not: `atoms/Slider.jsx` runs beside the DS's `molecules/Slider.jsx` at 10 import sites, only 2 of which need anything the DS lacks, and the local skin is verbatim `.control-slider` + `.slider-black` apart from track ink — which `--kol-slider-track` already exposes — plus a bordered pill the DS retired 2026-07-08. The 2026-08-27 pass renamed these out of the `kol-*` namespace (so nothing shadows the DS and theme bumps do reach here) but left the duplication. Two defects left unfixed on purpose in a file that is leaving: the in-thumb's `--kol-bg-surface-primary` is a token kol-theme has never defined, so it lives on a hardcoded `#0e0e11` and stays black in light mode; the playhead is a raw `#2dd4bf` |
| 2026-08-30 | **DS tier bumped again — component 0.131.0 → 0.133.0 · shell 0.19.1 → **0.22.0** · theme 0.96.0 → 0.97.0.** **The shell jump is BREAKING and the build caught it**: `SettingsSection` and `LabelRow` are gone from kol-shell's barrel (0.21.0, `SettingsScaffoldFromFxrPage` — DS ruling "dont ship duplicate components"; they were a second implementation of kol-component's `LabeledControlSection` + `SettingsRow`). `SettingsPage.jsx` moved onto the kol-component pair. Two rendering changes ride along and are the estate shape, not a regression: the row label is UPPERCASED by the component, and a row's control right-aligns — so the prose rows (Memory, Adaptive → Currently) pass `align="fill"` and keep their `text-fg-32 kol-helper-12` ink. Build green |
| 2026-08-30 | **`SliderDualThumbAndPlayhead` closed and adopted the same day** — component 0.133.0 + theme 0.97.0. **All 18 call sites across 10 files moved with no edit**; `atoms/Slider.jsx` is an adapter over `molecules/Slider` on the `molecules/Dropdown.jsx` pattern, pinning one thing (`readout="value"` — the DS default is an editable input chip, and the mixer runs 17 faders in 24px rows). `Slider-local.jsx` (183 lines) + every `.mirror-slider*` / `.mirror-range-*` rule (157 lines) → `_tmp/2026-08-30-slider-upstream/`; `kol-typography-mono.css:83` re-pointed to `.control-slider`. Lint fell 117 → 115 errors with the file. **The return's point 4 was moot**: `.mirror-slider`, the bordered pill, had ZERO call sites — all 18 passed `minimal` or `dual`. Playhead decided rather than restored by reflex: `#2dd4bf` at the one dual site via `[--kol-slider-playhead:#2dd4bf]`, because it sits among pause/clear/slot controls wearing that same teal — which is **not** mirror's accent (`#49A0A2`). This repo carries a second undocumented teal at nine sites |
| 2026-08-30 | **`SliderTrackVariableNotForwarded` filed to kol-ds-ui** — found adopting the above. The track-colour seam `Slider`'s own docstring documents cannot be passed: no `style` prop, no rest spread, and `.slider-black` declares `--kol-slider-track` on the element so inheritance is dead too. The same release's dual rail uses the fallback form and works, which is the fix. Carried here as one cascade rule, the wrong mechanism, recorded as such |
| 2026-08-30 | **The DS's hook-order warning was worth checking and did not come back empty.** kol-ds-ui flagged that the local Slider called `useMemo` after an early return and asked whether the shape existed elsewhere. `eslint react-hooks/rules-of-hooks` says yes — **4 more, all in `src/components/mirror/MirrorViewport.jsx`**: `CanvasFrame` hooks after `if (ratio === 'none') return children`, `CopiesViewport` hooks after `if (!Component) return null`. **Worse than the Slider's**, whose `variant` no call site ever switched — these two conditions change at runtime. Pre-existing, inside the standing lint baseline, **not fixed**: a live instrument file and the user's call |
| 2026-08-30 | **`SliderTrackVariableNotForwarded` closed and adopted, same day as filing** — theme 0.98.0 + component 0.134.0, both halves (the diagnosis that they were two independent blocks held: fixing one would have left the other route dead). Took the **per-instance `style`** rather than the wrapper the return suggested — mirror's faders span the mixer, the sidebar, VariantControls and three pages, so their only common ancestor is `:root`, and a `:root` declaration is the cascade rule under a nicer name. The adapter already existed and already pinned `readout`, so the second pin sits beside it, and a call site's own `style` spreads after and wins. `mirror-overrides.css` is back to the accent bind and the nav rule. Also took **shell 0.24.0** with it (fxr's `SettingsScaffold` tab-row work — additive; note `tone` now defaults to `sunken`) |
| 2026-08-30 | **Two tickets landed from kol-ds-ui** — `SettingsMastheadCluster` and `ShellSettingsPairRetired`, both about `/settings`. Neither took a ledger row on arrival; they existed only as files in `inbox/`, which is the raw-`ls` read this ledger exists to replace. Rowed 2026-09-01 |
| 2026-09-01 | **Both settings tickets addressed, neither closed.** Tier taken to the head in one move — kol-shell 0.26.0 → **0.31.0**, kol-theme 0.100.0 → **0.116.0**, kol-component 0.136.0 → **0.149.0**; the ticket asked for shell 0.27.0 + theme 0.106.0 and both were already four and ten releases back. The shell changelog documents 0.1.0–0.6.1 and then jumps to 0.31.0, so 0.7–0.30 were checked against the package SOURCE instead: all eleven kol-shell imports in `src/` survive the 0.31.0 barrel. `themeToggle` passed on fxr's approved row; the body's Display section dropped because its only row WAS the toggle and two on one page is the drift the ticket names. `onOpenSettings` deliberately not passed — mirror has no settings drawer, and its display settings are the Performance tab two rows below the gear. **Stopped at 🟠 on purpose:** the bar here is "verified by running it" and this session, like the two before it, never ran a dev server. kol-mirror is the FIRST repo anywhere to adopt the cluster, so the ticket's own "the first to adopt is the check" is unserved until the user looks |
| 2026-09-01 | **The held tier was taken — shell 0.31.0 → 0.35.0 · theme 0.116.0 → 0.121.0 · component 0.149.0 → 0.152.0 · framework 0.35.0 → 0.36.0.** All 17 named imports across the three barrels re-checked before installing; no call-site edit needed. Build green, lint 112/42, the baseline unmoved. **`ShellDrawerOnRight` is a non-event here**: shell 0.34.0 + theme 0.120.0 moved `touch="drawer"` to the right, 0.35.0 + 0.121.0 moved it back — the drawer opens LEFT, only the trigger is pinned top-right, so the mobile rail built the day before is unchanged. Skipped past two deprecated releases (component 0.150.0, a broken barrel; theme 0.120.0, a duplicate class name). Also in: `CatalogPageMobileColumns` (shell 0.33.0), `SettingsShortcutsComboOverflow` (shell 0.33.0 + component 0.151.0) and `--kol-shell-page-pad` going responsive (theme 0.118.0) — three mobile fixes landing on surfaces this repo enabled the day before, none of them seen. Both settings tickets stay 🟠🔴: the tier they were addressed on has moved, the bar has not. |
| 2026-09-01 | **The repo was looked at on a phone for the first time, and three DS gaps came out of it.** A sanctioned dev server (the user's, on his own handset) plus a Playwright pass at 390×844 — four sessions of mobile work had shipped with nothing on screen. Fixed here: `/studio` opened on **"Needs a bigger screen"** because `useMirrorState` defaults `activeHall` to `'symphony'` and `MobileStudio` never shimmed it, so the phone's first ever render was a dead end over an undrawn canvas; `/library` drew its own inline `repeat(6, 1fr)` and gave six 38px slivers with every title cut to three characters; the five Pixi grab surfaces had no `touch-action`, so the one gesture those variants have was eaten by the scroller. Filed out: `SliderCoarsePointerHeight` · `ShellDrawerBottomSide` · `SettingsShortcutsGridColumns` — receipts above. **`CatalogPageMobileColumns` (kol-monitor → shell 0.33.0) paid off here unprompted**: Home renders correctly at 390 with no work, which is the argument for filing rather than fixing locally |
| 2026-09-01 | **Three tickets filed to kol-ds-ui in the morning were closed, bumped and verified by evening — and the two standing inbox tickets closed with them.** `SliderCoarsePointerHeight` → **theme 0.122.0** (`touch-action: pan-y` on the input, 24 → 44 under `pointer: coarse`, plus a `.control-slider` row lift mirror could not have seen because `VariantControls` passes `rowHeight` inline — *"a fix that only works with a consumer override is the override wearing a DS badge"*). `ShellDrawerBottomSide` → **component 0.153.0**, adopted the same sitting: `MobileStudio`'s hand-rolled sheet is now `<ShellDrawer side="bottom" height="68dvh" backdrop={false}>`, measured at y 270 / h 574 = exactly 68dvh, and it arrives with the portal, Escape, focus trap, scroll lock and reduced-motion gate the local `<div>` never had; the collapsed 56px bar stays local because the sheet ships ONE detent and said so rather than half-building a second. `SettingsShortcutsGridColumns` → **shell 0.36.0**, and they swept the shell for `grid-cols-` afterwards — it was the last. **Both inbox tickets went 🟢 on the same evidence**: `SettingsMastheadCluster` and `ShellSettingsPairRetired` had been 🔴 on nothing but the user's screen since 2026-08-30, and this is the first browser pass this repo has ever had |
| 2026-09-01 | **The user looked at it on his phone and four things came back — three fixed, one reopened the scope.** (1) The rail was folded everywhere; `drawerOpen` was internal to `AppShell` with no way in, so it went to the DS as `ShellDrawerOpenOnRoute` and came back as **kol-shell 0.37.0** `drawerOpenOn` — home now lands with the nav out, every other route folded, on his ruling *"the rail should load open on home, not everywhere"*. (2) The sheet's contents were still raw `<button>`s after I filed only the CONTAINER gap and wrote in the plan that "the content is already right" — it was not; the hall picker is `SegmentedToggle`, the variant list is `ContentRow variant="file"`, the picker is `Button`. Two measured layout bugs fell out of that swap: `ShellDrawer` pads its own panel 16 and our `px-4` doubled it, and `size="lg"` wanted 372px in a 358px row and clipped "Copies". (3) The controls rendered BELOW the eight-row variant list, so the knobs for the effect you just chose were the furthest thing from your thumb — they now sit directly under the hall row. (4) **The scope premise itself was wrong.** "Mobile is the halls, not the desk" came from the PREVIOUS session's log, where it is recorded as a user ruling; this session inherited it as law and built the plan on it, including redirecting `/mixer`, `/expressions` and `/tape` away — and was told three messages earlier to stop treating a logged line as law. **Nothing is omitted from mobile now**; the mixer is its own arc |
| 2026-09-01 | **The create mixer, the module shelf, and the standing rule that produced them.** User, in caps after three rounds of me guessing at shapes monitor had already settled: *"REFERENCE MONITOR ALWAYS INSTEAD OF GUESSING"* — read kol-monitor's actual file before any structural decision here. Built on that: **CREATE's builder is now a three-channel mixer** (`MixerBuilder`, monitor's CASE ⇄ MODULES split from `CreatePage.jsx:204`), the catalogue feeds the active channel, and one handover opens the whole desk — verified end to end, two channels landing with their FX and the third left untouched. **The module shelf got a touch entry** — ⌘K and E are keys, so the registry was unreachable on a phone; monitor has the same gap (`useKeybindings.js:16`) so there was nothing to port, and what was followed instead is its idiom for desk chrome, a fixed corner control. ONE forced difference from monitor is recorded in the code: `CatalogPage` maps `viewModeOptions` to its own `views`, so mirror's two build views sit one slot down or Recent · Saved · Create vanish. Also fixed: **kol-shell 0.37.0 made the rail unopenable** — `drawerOpenOn` defaults to a fresh `[]` inside its effect's dep array, so every tap was undone on the next render; found by the user on his phone, filed as `ShellDrawerOpenOnUnstableDep` 🔴, carried locally with one frozen array. `ShellRailCollapsedWithTapOpen` filed for the actual ruling — a COLLAPSED rail that opens on a tap, which no `touch` mode provides |
| 2026-09-01 | **Caught not following my own outbox.** The user asked whether I was tracking the tickets I file; I was not. `ShellRailCollapsedWithTapOpen` and `ShellDrawerOpenOnUnstableDep` had both been filed to kol-ds-ui, both CLOSED there while I was building the create page, and neither had a receipt stub or a ledger row on this side — they existed only as files in another repo's `done/`, which is the raw-`ls` read this ledger exists to replace. Both remainders were owed and unstarted. Squared: bumped **kol-shell 0.38.0 + kol-theme 0.123.0**, took the per-route `touch` the tap disc finally makes possible, deleted the frozen-array workaround, wrote both receipts, and re-synced FOUR stub headers that still read 🔵 `filed` against a ledger that had them 🟢 — `ContentCardExpandedSplitStacks`, `ShellDrawerOpenOnRoute`, and both Sliders from 2026-08-30 |
| 2026-09-02 | **`ContentFiltersViewStripOverflow` filed to kol-ds-ui** — the Library became five views on monitor's kinds-as-views shape and the DS view strip clipped at 390 (440px in a 390 page, two views unreachable). LIST / GRID already has a narrow placement; the view family has none. Carried here as one scoped cascade rule under `.library-catalog`, the wrong mechanism, recorded as such |
| 2026-09-02 | **`ContentFiltersViewStripOverflow` closed and adopted, same day as filing** — kol-component 0.163.0, `viewPlacement`. The scoped cascade rule and its class are gone; at 390 the five views wrap onto two rows under the title, all reachable, at 1280 they are back on the title row |
